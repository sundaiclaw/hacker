# Roadmap

> Generated view of the release train. **Not a second source of truth.**
> Authoritative state lives in `openspec/major/` (per-major plans) and `openspec/changes/` (active minor slices).

## Active major

- [`openspec/major/v0/plan.md`](openspec/major/v0/plan.md)

## Active minor slice

- [`openspec/changes/v0.1/`](openspec/changes/v0.1/)

## Shipped

_Populated as `vX.Y.0` tags land. Each line: tag, one-line summary, link to archived change._

- _none yet_

## Next up

See the active major plan's "Minor slices" table and "Invalidation log" for the canonical order and any stale slices.
